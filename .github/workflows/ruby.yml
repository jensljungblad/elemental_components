name: Ruby

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        ruby:
          - '2.5.9'
          - '2.6.10'
          - '2.7.8'
          - '3.0.6'
          - '3.1.4'
          - '3.2.2'
        gemfile:
          - 'rails_5'
          - 'rails_6'
    steps:
    - uses: actions/checkout@v4
    - uses: ruby/setup-ruby@v1
      with:
        ruby-version: ${{ matrix.ruby }}
        bundler-cache: true
    - name: Build and test
      run: |
        bundle exec rubocop
        bundle exec rake
