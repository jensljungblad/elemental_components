<% Components.component_names.each do |name| %>
  <% begin %>
    <% require_asset "#{name}/#{name.split('/')[-1]}" %>
  <% rescue Sprockets::FileNotFound %>
    <% Rails.logger.debug "Components: CSS not found for #{name}" %>
  <% end %>
<% end %>
